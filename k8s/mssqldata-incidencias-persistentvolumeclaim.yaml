apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mssqldata-incidencias
  name: mssqldata-incidencias
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
